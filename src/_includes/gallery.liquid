---
layout: base.liquid
---
<div class='container gallery mb-20 mt-20 gap-4'>
  {% for item in images %}
    <a data-lightbox='mygallery' href='{{item}}' class='block relative'>
      <img alt='' class='rounded-2xl' src='{{item}}'>
    </a>
  {% endfor %}
</div>